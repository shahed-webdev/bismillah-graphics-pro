@{
    ViewData["Title"] = "Selling due pay";
    Layout = "_AdminLayout";
}

<div x-data="initData()" x-init="getDropdownData">
    <div class="container">
        <div class="card border-0 shadow-sm mt-3">
            <div class="card-body pb-2">
                <div class="d-flex align-items-center">
                    <div class="d-flex align-items-center justify-content-center rounded-circle purple-gradient white-text mr-3 flex-shrink-0" style="width:48px;height:48px;">
                        <i class="fas fa-building"></i>
                    </div>
                    <div>
                        <h5 x-text="model.VendorCompanyName" class="mb-1 font-weight-bold"></h5>
                        <p class="mb-0 text-muted small">
                            <i class="fas fa-map-marker-alt mr-1"></i><span x-text="model.VendorAddress"></span>
                            <span class="mx-2">|</span>
                            <i class="fas fa-phone-alt mr-1"></i><span x-text="model.SmsNumber"></span>
                        </p>
                    </div>
                </div>
            </div>
            <div class="card-footer bg-light border-0 px-4 py-3">
                <form x-on:submit.prevent="finByDate">
                    <div class="form-row align-items-end">
                        <div class="col-12 col-md-auto mb-2 mb-md-0 d-flex align-items-center">
                            <span class="small font-weight-bold text-muted text-uppercase mb-0" style="letter-spacing:.04em;">
                                <i class="fas fa-filter mr-1"></i>Filter Bills
                            </span>
                        </div>
                        <div class="col-md">
                            <label class="small font-weight-bold text-muted mb-1">From Date</label>
                            <div class="input-group">
                                <div class="input-group-prepend">
                                    <span class="input-group-text bg-white"><i class="fas fa-calendar-alt text-muted"></i></span>
                                </div>
                                <input x-ref="fromDate" class="form-control datepicker bg-white" placeholder="From Date" type="text">
                            </div>
                        </div>
                        <div class="col-md">
                            <label class="small font-weight-bold text-muted mb-1">To Date</label>
                            <div class="input-group">
                                <div class="input-group-prepend">
                                    <span class="input-group-text bg-white"><i class="fas fa-calendar-alt text-muted"></i></span>
                                </div>
                                <input x-ref="toDate" class="form-control datepicker bg-white" placeholder="To Date" type="text">
                            </div>
                        </div>
                        <div class="col-md-auto mt-2 mt-md-0">
                            <button class="btn btn-custom-bg btn-md btn-block px-4 my-0" type="submit">
                                <i class="fas fa-search mr-1"></i>Search
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <form x-on:submit.prevent="payDues">
            <div class="row my-4 align-items-stretch">
                <div class="col-md-5 mb-3 mb-md-0">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-header bg-white border-0 pt-3 pb-0">
                            <h6 class="text-uppercase text-muted font-weight-bold mb-0" style="letter-spacing:.05em;">Account Summary</h6>
                        </div>
                        <div class="card-body d-flex flex-column justify-content-center py-3">
                            <div class="d-flex justify-content-between align-items-center py-2 border-bottom">
                                <span class="text-muted"><i class="fas fa-file-invoice-dollar mr-2 text-primary"></i>Sales</span>
                                <span class="font-weight-bold">৳<span x-text="model.Amount"></span></span>
                            </div>
                            <div class="d-flex justify-content-between align-items-center py-2 border-bottom">
                                <span class="text-muted"><i class="fas fa-money-bill-wave mr-2 text-success"></i>Received</span>
                                <span class="font-weight-bold text-success">৳<span x-text="model.Paid"></span></span>
                            </div>
                            <div class="d-flex justify-content-between align-items-center py-2 border-bottom">
                                <span class="text-muted"><i class="fas fa-tags mr-2 text-warning"></i>Total Discount</span>
                                <span class="font-weight-bold">৳<span x-text="model.Discount"></span></span>
                            </div>
                            <div class="d-flex justify-content-between align-items-center mt-3 p-3 rounded" style="background-color:#fff3f3;">
                                <span class="font-weight-bold text-danger"><i class="fas fa-exclamation-circle mr-2"></i>Total Due</span>
                                <span class="h4 mb-0 font-weight-bold text-danger">৳<span x-text="model.Due"></span></span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-7">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="purple-gradient darken-2 white-text px-4 py-3 rounded-top">
                            <h5 class="mb-0 font-weight-bold"><i class="fas fa-wallet mr-2"></i>Payment Details</h5>
                        </div>
                        <div class="card-body py-4">
                            <div class="form-group">
                                <label class="font-weight-bold mb-1">Paid Amount</label>
                                <div class="input-group">
                                    <div class="input-group-prepend">
                                        <span class="input-group-text">৳</span>
                                    </div>
                                    <input x-model.number="model.PaidAmount" x-on:input="inputPaidAmount" x-ref="totalPaid" type="number" step="0.01" min="0.01" :max="model.Due" class="form-control text-right font-weight-bold" required />
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="font-weight-bold mb-1">Payment Method</label>
                                <select x-model.number="model.AccountId" class="form-control" required>
                                    <option value="">[ SELECT ]</option>
                                    <template x-for="account in accounts" :key="account.value">
                                        <option :value="account.value" x-text="account.label"></option>
                                    </template>
                                </select>
                            </div>

                            <div class="form-group">
                                <label class="font-weight-bold mb-1">Paid Date</label>
                                <div class="input-group">
                                    <div class="input-group-prepend">
                                        <span class="input-group-text bg-white"><i class="fas fa-calendar-alt text-muted"></i></span>
                                    </div>
                                    <input x-ref="datePicker" :value="moment().format('D MMMM, YYYY')" type="text" class="form-control datepicker" required>
                                </div>
                            </div>

                            <div class="form-group mb-0">
                                <label class="font-weight-bold mb-1">Description</label>
                                <input x-model="model.Description" type="text" class="form-control" placeholder="Optional note" />
                            </div>

                            <div class="text-right mt-4">
                                <button :disabled="loading" type="submit" class="btn btn-custom-bg btn-md px-4">
                                    <i class="fas fa-check mr-1"></i> Submit
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card card-body">
                <table class="table table-bordered table-sm">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th class="text-center">Bill No</th>
                            <th class="text-right">Sales</th>
                            <th class="text-right">Discount</th>
                            <th class="text-right">Received</th>
                            <th class="text-right">Due Balance</th>
                            <th class="text-right">Paid</th>
                        </tr>
                    </thead>
                    <tbody>
                        <template x-for="(item,index) in model.Sellings" :key="item.SellingId">
                            <tr>
                                <td x-text="moment(item.SellingDate).format('D MMM, YYYY')"></td>
                                <td class="text-center" x-text="item.SellingSn"></td>
                                <td class="text-right">
                                    ৳<span x-text="item.SellingTotalPrice"></span>
                                </td>
                                <td class="text-right" style="max-width: 100px">
                                    <input x-model.number="item.SellingDiscountAmount" x-on:input="onDiscountChange(index)" type="number" class="form-control text-right" min="0" :max="item.SellingTotalPrice-item.SellingPaidAmount" step="0.01" placeholder="Discount amount">
                                </td>
                                <td class="text-right">
                                    ৳<span x-text="item.SellingPaidAmount"></span>
                                </td>
                                <td class="text-right">
                                    ৳<span x-text="item.SellingDueAmount"></span>
                                </td>
                                <td class="text-right">
                                    ৳<span x-text="item.Paying || 0"></span>
                                </td>
                            </tr>
                        </template>
                    </tbody>
                </table>
            </div>
        </form>
    </div>
</div>



@section Scripts {
<script>
    $(function() {
        $('.datepicker').pickadate();
    });

    //alpine js code
    function initData() {
        return {
            loading: false,
            model: JSON.parse('@Html.Raw(Json.Serialize(ViewBag.dueModel))'),
         

            //dropdown list
            accounts: [],

            //get dropdown data
            async getDropdownData() {
                console.log('@Html.Raw(Json.Serialize(ViewBag.dueModel))')

                //reset paid amount after reload
                this.$refs.totalPaid.value = "";

                const accountResponse = await fetch("/common/getAccount");
                const account = await accountResponse.json();
                this.accounts = account;
            },



            //bill find by date
            async finByDate() {
                const fromDate = this.$refs.fromDate.value;
                const toDate = this.$refs.toDate.value;
                const id = this.model.VendorId;

                const response = await fetch(`/Selling/GetDueBills?id=${id}&from=${fromDate}&to=${toDate}`);
                const responseData = await response.json();

                if (responseData.IsSuccess)
                    this.model = responseData.Data;
            },

            //discount amount change
            onDiscountChange(index) {
                const modelCopy = [...this.model.Sellings];
                const item = modelCopy[index];
                const due = item.SellingTotalPrice - item.SellingDiscountAmount - item.SellingPaidAmount;

                modelCopy.forEach((item, i) => {
                    if (i === index) item.SellingDueAmount = due;

                    item.Paying = 0;
                });

                this.model.Sellings = modelCopy;
                this.$refs.totalPaid.value = "";
            },

            //input total paid
            inputPaidAmount(evt) {
                let totalPaid = +evt.target.value;

                const SellingDues = [...this.model.Sellings];

                SellingDues.forEach(item => {
                    const lineDue = item.SellingDueAmount;

                    if (totalPaid < lineDue && totalPaid > 0) {
                        item.Paying = totalPaid;
                    } else if (totalPaid >= lineDue) {
                        item.Paying = lineDue;
                    } else {
                        item.Paying = 0;
                    }

                    totalPaid -= lineDue;
                });

                this.model.Sellings = SellingDues;
            },

            //submit dues
            payDues() {
                const { VendorId, AccountId, PaidAmount, Description, Sellings } = this.model;

                const model = {
                    VendorId,
                    AccountId,
                    PaidAmount,
                    PaidDate:this.$refs.datePicker.value,
                    Description,
                    Bills: Sellings.filter(item => item.Paying > 0).map(item => {
                        return {
                            SellingId: item.SellingId,
                            SellingPaidAmount: item.Paying,
                            SellingDiscountAmount: item.SellingDiscountAmount
                        }
                    })
                }

                if (!model.Bills.length) {
                    return $.notify("Enter paid amount", "error");
                }

                this.loading = true;

                $.ajax({
                    type: "POST",
                    url: "/Selling/PostDues",
                    data: model,
                    success: response => {
                        if (response.IsSuccess) {
                            location.href = `/selling/paymentReceipt/${response.Data}`;
                        }

                        $.notify(response.Message, response.IsSuccess ? "success" : "error");
                        this.loading = false;
                    },
                    error: error => {
                        console.log(error);
                        this.loading = false;
                    }
                });
            }
        };
    }
</script>
}
