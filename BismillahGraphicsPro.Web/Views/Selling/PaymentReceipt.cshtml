@model SellingPaymentReceiptViewModel
@{
    ViewData["Title"] = "Selling Payment Receipt";
    Layout = "_AdminLayout";
}


<section class="my-4 d-print-none">
    <a class="btn btn-brown btn-rounded" href="/Selling/paymentReport"><i class="fas fa-shopping-cart mr-1"></i>Payment Report</a>
    <button class="btn btn-deep-purple btn-rounded" onclick="print()"><i class="fas fa-print mr-1"></i>Print</button>
</section>


<div class="card card-body">
    <div class="row my-2 align-items-center">
        <div class="col">
            <span>RECEIPT# @Model.ReceiptSn</span>
        </div>
        <div class="col text-center">
            <strong>SELLING BILLS</strong>
        </div>
        <div class="col text-right">
            <span>Date: @Model.PaidDate.ToString("d MMM yyyy")</span>
        </div>
    </div>

    <div class="row mb-2">
        <div class="col-7">
            <h6><strong>@Model.VendorCompanyName</strong></h6>
            <h6><strong>@Model.VendorName</strong></h6>
        </div>
        <div class="col text-right">
            @if (!string.IsNullOrEmpty(Model.VendorAddress))
            {
                <h6><strong>@Model.VendorAddress</strong></h6>
            }
            <h6><strong>@Model.SmsNumber</strong></h6>
        </div>
    </div>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th class="text-center"><strong>No.</strong></th>
                <th><strong>Selling Date</strong></th>
                <th class="text-center"><strong>Bill No.</strong></th>
                <th class="text-right"><strong>Amount</strong></th>
                <th class="text-right"><strong>Paid</strong></th>
            </tr>
        </thead>
        <tbody>
            @{
                var index = 0;
                foreach (var item in Model.Bills)
                {
                    index++;
                    <tr>
                        <td class="text-center">@index</td>
                        <td>@item.SellingDate.ToString("d MMM yyyy")</td>
                        <td class="text-center">
                            <a class="blue-text" href="/Selling/receipt/@item.SellingId">
                                @item.SellingSn
                            </a>
                        </td>
                        <td class="text-right">@item.SellingTotalPrice/-</td>
                        <td class="text-right">@item.SellingPaidAmount/-</td>
                    </tr>
                }
            }
            <tr>
                <td colspan="4">
                    <span id="amount-word"></span>
                </td>
                <td colspan="2">
                    <div class="text-right">
                        <h5>Total Paid: <strong>@Model.PaidAmount/-</strong></h5>
                    </div>
                </td>
            </tr>
        </tbody>
    </table>
</div>